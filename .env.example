# Copy to .env and adjust as needed.
DATA_DIR=./data
HOST=127.0.0.1
PORT=8000
# Set to 1 to allow non-loopback bind. Default 0 refuses to start on anything
# other than 127.0.0.1.
ALLOW_REMOTE=0
LOG_LEVEL=info
